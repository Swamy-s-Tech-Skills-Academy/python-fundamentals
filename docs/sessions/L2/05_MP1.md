---
learning_level: "Nerd → Novice"
prerequisites:
  - "docs/sessions/L2/01_S1.md"
  - "docs/sessions/L2/02_S2.md"
  - "docs/sessions/L2/03_S3.md"
  - "docs/sessions/L2/04_S4.md"
estimated_time: "30-45 minutes"
session_type: "Project"
learning_objectives:
  - "Apply sets, tuples, and list comprehensions in a real project"
  - "Use functions to organize code"
  - "Handle errors gracefully with try/except"
  - "Process and transform data effectively"
  - "Build a complete, working application"
related_topics:
  prerequisites:
    - "docs/sessions/L2/04_S4.md"
  builds_upon:
    - "docs/sessions/L2/01_S1.md"
    - "docs/sessions/L2/02_S2.md"
    - "docs/sessions/L2/03_S3.md"
    - "docs/sessions/L2/04_S4.md"
  enables:
    - "docs/sessions/L2/06_S5.md"
  cross_refs: []
---

# 🚀 Mini Project 1: Data Processor

**Duration:** 30-45 minutes  
**Type:** 🛠️ Project  
**Level:** Nerd → Novice

---

## 🎯 **Project Overview**

### **What You'll Build:**

A data processing application that demonstrates your mastery of Phase A concepts:

- **Sets**: Remove duplicates from data
- **Tuples**: Store structured data
- **List Comprehensions**: Transform data elegantly
- **Functions**: Organize code into reusable pieces
- **Error Handling**: Make the program robust

### **Project Goal:**

Build a program that processes a list of student scores, removes duplicates, calculates statistics, and displays results using all Phase A concepts.

---

## 📋 **Before You Begin**

### **Prerequisites:**

✅ Completed [Session 1: Advanced Data Structures](docs/sessions/L2/01_S1.md)  
✅ Completed [Session 2: List Comprehensions](docs/sessions/L2/02_S2.md)  
✅ Completed [Session 3: Functions: Definition & Basics](docs/sessions/L2/03_S3.md)  
✅ Completed [Session 4: Error Handling](docs/sessions/L2/04_S4.md)  
✅ Understand all Phase A concepts

---

## 🎯 **Project Requirements**

### **Core Features (Must Have):**

1. **Data Input**
   - Accept a list of student scores (can be hardcoded or user input)
   - Handle invalid input gracefully with error handling

2. **Data Processing**
   - Remove duplicates using sets
   - Store processed data using tuples
   - Transform data using list comprehensions

3. **Functions**
   - Organize code into at least 3 functions:
     - Function to get/validate input
     - Function to process data
     - Function to display results

4. **Error Handling**
   - Handle invalid input (non-numbers)
   - Handle empty data
   - Show user-friendly error messages

5. **Output**
   - Display original data
   - Display unique scores (using sets)
   - Display statistics (min, max, count)
   - Display processed data using tuples

### **Stretch Goals (Optional):**

- Calculate average score
- Filter scores above a threshold
- Sort scores in different orders
- Export results to a formatted string
- Add more statistics (median, mode)

---

## 💻 **Step-by-Step Implementation**

### **Step 1: Plan Your Functions**

Think about what functions you need:

```python
def get_scores():
    # Get and validate input
    pass

def process_scores(scores):
    # Remove duplicates, create tuples, transform data
    pass

def display_results(original, processed):
    # Show results nicely
    pass
```

### **Step 2: Implement Data Input Function**

```python
def get_scores():
    """Get scores from user with error handling."""
    try:
        # Get input (you can also use hardcoded data for testing)
        scores_input = input("Enter scores separated by commas: ")
        scores = [int(score.strip()) for score in scores_input.split(",")]
        return scores
    except ValueError:
        print("Error: Please enter valid numbers separated by commas!")
        return []
    except Exception as e:
        print(f"Unexpected error: {e}")
        return []
```

### **Step 3: Implement Data Processing Function**

```python
def process_scores(scores):
    """Process scores using sets, tuples, and comprehensions."""
    if not scores:
        return None, None, None
    
    # Remove duplicates using set
    unique_scores = set(scores)
    
    # Create tuples of (score, frequency) using list comprehension
    score_tuples = [(score, scores.count(score)) for score in unique_scores]
    
    # Transform: get only scores above 70 using comprehension
    passing_scores = [score for score in unique_scores if score >= 70]
    
    return unique_scores, score_tuples, passing_scores
```

### **Step 4: Implement Display Function**

```python
def display_results(original, unique, tuples, passing):
    """Display all results in a formatted way."""
    print("=" * 50)
    print("DATA PROCESSING RESULTS")
    print("=" * 50)
    
    print(f"\nOriginal Scores: {original}")
    print(f"Unique Scores (Set): {sorted(unique)}")
    print(f"Score Tuples (Score, Count): {tuples}")
    print(f"Passing Scores (>=70): {sorted(passing)}")
    
    if unique:
        print(f"\nStatistics:")
        print(f"  Total unique scores: {len(unique)}")
        print(f"  Minimum score: {min(unique)}")
        print(f"  Maximum score: {max(unique)}")
```

### **Step 5: Put It All Together**

```python
# Main program
def main():
    scores = get_scores()
    
    if not scores:
        print("No valid scores to process.")
        return
    
    unique, tuples, passing = process_scores(scores)
    display_results(scores, unique, tuples, passing)

# Run the program
if __name__ == "__main__":
    main()
```

---

## 🧪 **Complete Example Solution**

🧪 *Deliverable*: `src/L2/MP1/data_processor.py`

Here's a complete working example:

```python
# Filename: src/L2/MP1/data_processor.py
# Mini Project 1: Data Processor

def get_scores():
    """Get scores from user with error handling."""
    try:
        scores_input = input("Enter scores separated by commas: ")
        if not scores_input.strip():
            print("No input provided. Using sample data.")
            return [85, 90, 85, 92, 90, 88, 85, 75, 95, 88]
        
        scores = [int(score.strip()) for score in scores_input.split(",")]
        return scores
    except ValueError:
        print("Error: Please enter valid numbers separated by commas!")
        print("Using sample data instead.")
        return [85, 90, 85, 92, 90, 88, 85, 75, 95, 88]
    except Exception as e:
        print(f"Unexpected error: {e}")
        return []


def process_scores(scores):
    """Process scores using sets, tuples, and comprehensions."""
    if not scores:
        return None, None, None, None
    
    # Remove duplicates using set
    unique_scores = set(scores)
    
    # Create tuples of (score, frequency) using list comprehension
    score_tuples = [(score, scores.count(score)) for score in unique_scores]
    
    # Transform: get only scores above 70 using comprehension
    passing_scores = [score for score in unique_scores if score >= 70]
    
    # Calculate average using comprehension and built-in functions
    average = sum(unique_scores) / len(unique_scores) if unique_scores else 0
    
    return unique_scores, score_tuples, passing_scores, average


def display_results(original, unique, tuples, passing, average):
    """Display all results in a formatted way."""
    print("=" * 50)
    print("DATA PROCESSING RESULTS")
    print("=" * 50)
    
    print(f"\nOriginal Scores: {original}")
    print(f"Total scores: {len(original)}")
    
    print(f"\nUnique Scores (Set): {sorted(unique)}")
    print(f"Total unique scores: {len(unique)}")
    
    print(f"\nScore Tuples (Score, Frequency):")
    for score, count in sorted(tuples):
        print(f"  {score}: appears {count} time(s)")
    
    print(f"\nPassing Scores (>=70): {sorted(passing)}")
    print(f"Passing count: {len(passing)}")
    
    if unique:
        print(f"\nStatistics:")
        print(f"  Minimum score: {min(unique)}")
        print(f"  Maximum score: {max(unique)}")
        print(f"  Average score: {average:.2f}")


def main():
    """Main program function."""
    print("=" * 50)
    print("STUDENT SCORE DATA PROCESSOR")
    print("=" * 50)
    print()
    
    scores = get_scores()
    
    if not scores:
        print("No valid scores to process. Exiting.")
        return
    
    unique, tuples, passing, average = process_scores(scores)
    
    if unique is None:
        print("Error processing scores.")
        return
    
    display_results(scores, unique, tuples, passing, average)
    
    print("\n" + "=" * 50)
    print("Processing complete!")
    print("=" * 50)


# Run the program
if __name__ == "__main__":
    main()
```

---

## ✅ **Success Criteria**

Your project is successful if it:

- ✅ Accepts input (or uses hardcoded data) with error handling
- ✅ Removes duplicates using sets
- ✅ Uses tuples to store structured data
- ✅ Uses list comprehensions to transform data
- ✅ Organizes code into at least 3 functions
- ✅ Handles errors gracefully (no crashes)
- ✅ Displays results clearly
- ✅ Code runs without errors

---

## 🎯 **Stretch Goals**

If you finish early, try adding:

1. **Calculate Statistics**

   ```python
   # Add median, mode calculations
   ```

2. **Filter Options**

   ```python
   # Allow user to filter by different thresholds
   ```

3. **Export Results**

   ```python
   # Format results as a string for saving
   ```

4. **Interactive Menu**

   ```python
   # Let user choose what to process
   ```

---

## 🔧 **Troubleshooting**

### **Common Issues:**

**Issue:** Function not being called  
**Solution:** Make sure you call `main()` at the end with `if __name__ == "__main__":`

**Issue:** Set operations not working  
**Solution:** Remember sets are unordered - use `sorted()` if you need order

**Issue:** Comprehension syntax error  
**Solution:** Check brackets - list comprehensions use `[]`

**Issue:** Error handling not catching errors  
**Solution:** Make sure exception types match the actual errors

---

## 🔗 **What's Next?**

Congratulations on completing Phase A! You've built a working application using sets, tuples, list comprehensions, functions, and error handling.

**Next Up:**

- [Session 5: Functions: Parameters & Return Values](docs/sessions/L2/06_S5.md) - Make functions more powerful
- [Session 6: Functions: Scope & Code Organization](docs/sessions/L2/07_S6.md) - Organize larger programs

---

✨ Happy Coding! 🐍
