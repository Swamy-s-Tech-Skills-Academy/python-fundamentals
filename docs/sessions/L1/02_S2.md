---
learning_level: "Noob → Nerd"
prerequisites: 
  - "docs/sessions/L1/01_S1.md"
estimated_time: "30 minutes"
session_type: "Knowledge"
learning_objectives:
  - "Understand variable naming conventions and assignment"
  - "Learn Python's fundamental data types: int, float, str, bool"
  - "Understand dynamic typing in Python"
  - "Use type() and isinstance() to check data types"
  - "Perform type conversion and casting between data types"
  - "Store and display personal details using different data types"
related_topics:
  prerequisites:
    - "docs/sessions/L1/01_S1.md"
  builds_upon:
    - "docs/sessions/L1/01_S1.md"
  enables:
    - "docs/sessions/L1/03_S3.md"
  cross_refs: []
---

# Session 2: Variables & Data Types

**Duration:** 30 minutes  
**Type:** 📚 Knowledge  
**Level:** Noob → Nerd

---

## 🎯 **Session Overview & Agenda**

### **What You'll Learn:**

- ✅ Understand what variables are and how to name them properly
- ✅ Learn Python's four fundamental data types: `int`, `float`, `str`, `bool`
- ✅ Understand dynamic typing (Python figures out types automatically)
- ✅ Use `type()` and `isinstance()` to check data types
- ✅ Convert between different data types (type conversion/casting)
- ✅ Practice storing and displaying personal information

### **Session Flow:**

| Part | Activity                        | Description                                                                                |
| ---- | ------------------------------- | ------------------------------------------------------------------------------------------ |
| 1    | Variables & Naming              | What are variables? How to name them properly                                              |
| 2    | Data Types Introduction          | Meet Python's four fundamental data types                                                  |
| 3    | Dynamic Typing                  | How Python automatically determines data types                                              |
| 4    | Type Checking                   | Using `type()` and `isinstance()` to verify data types                                      |
| 5    | Type Conversion                 | Converting between different data types                                                    |
| 6    | Hands-on Practice               | Build a personal details program using all data types                                       |

---

## 📋 **Before You Begin**

### **Prerequisites:**

✅ Completed Session 1: Python Introduction & Environment Setup  
✅ Understand how to use `print()` and `input()`  
✅ Know how to run Python scripts  
✅ Familiar with basic Python syntax

---

## 💻 **Part 1: Variables & Naming**

### **What is a Variable?**

A **variable** is like a labeled box where you store information. You give it a name, and Python remembers what's inside.

Think of it like this:

- **Variable name** = Label on the box
- **Variable value** = What's inside the box

### **Creating Variables (Assignment)**

In Python, you create variables using the **assignment operator** (`=`):

```python
name = "Alice"
age = 25
```

**How to read this:**

- `name = "Alice"` means "store the value 'Alice' in a variable called 'name'"
- `age = 25` means "store the value 25 in a variable called 'age'"

### **Variable Naming Rules**

Python has specific rules for naming variables:

✅ **Allowed:**

- Letters (a-z, A-Z)
- Numbers (0-9) - but NOT at the start
- Underscores (_)
- Case-sensitive (Age and age are different)

❌ **Not Allowed:**

- Spaces
- Special characters (@, #, $, %, etc.)
- Starting with a number
- Python keywords (like `print`, `if`, `for`, etc.)

**Good Variable Names:**

```python
first_name = "Alice"
age_in_years = 25
is_student = True
total_score = 95.5
```

**Bad Variable Names:**

```python
first name = "Alice"    # ❌ Space not allowed
2nd_place = "Bob"       # ❌ Can't start with number
print = "Hello"         # ❌ 'print' is a Python keyword
```

### **Naming Conventions (Best Practices)**

While Python allows many names, following conventions makes code readable:

1. **Use descriptive names**: `age` is better than `a`
2. **Use lowercase with underscores**: `first_name` not `FirstName`
3. **Be consistent**: If you use `user_name`, don't switch to `username` later
4. **Avoid abbreviations**: `age` is better than `ag`

**Examples:**

```python
# ✅ Good naming
student_name = "Alice"
student_age = 20
is_enrolled = True

# ❌ Less clear
sn = "Alice"
sa = 20
ie = True
```

### ✅ **Progress Check: Variables**

Before moving on, ensure you understand:

- [ ] Variables store values with names
- [ ] Assignment uses `=` (name goes on left, value on right)
- [ ] Variable names follow Python's rules
- [ ] Good names are descriptive and readable

---

## 📊 **Part 2: Data Types Introduction**

Python has four **fundamental data types** you'll use constantly:

### **1. Integers (`int`)**

Whole numbers (positive, negative, or zero):

```python
age = 25
temperature = -10
count = 0
population = 8000000000
```

### **2. Floating-Point Numbers (`float`)**

Numbers with decimal points:

```python
height = 5.9
price = 19.99
temperature = 98.6
pi = 3.14159
```

**Note:** Even if you write `5.0`, Python treats it as a float, not an integer.

### **3. Strings (`str`)**

Text data (always in quotes):

```python
name = "Alice"
message = 'Hello, World!'
favorite_color = "blue"
```

**String Rules:**

- Use single quotes `'text'` or double quotes `"text"`
- Both work the same way
- Be consistent in your code

### **4. Booleans (`bool`)**

True or False values (exactly two options):

```python
is_student = True
is_graduated = False
has_license = True
```

**Important:**

- `True` and `False` must start with capital letters
- No quotes around them (they're not strings!)

### **Quick Reference Table**

| Data Type | Example Values | Use For |
| --------- | -------------- | ------- |
| `int` | `25`, `-10`, `0`, `1000` | Whole numbers |
| `float` | `3.14`, `-5.5`, `0.0`, `99.9` | Decimal numbers |
| `str` | `"Hello"`, `'Python'`, `"123"` | Text data |
| `bool` | `True`, `False` | Yes/No, On/Off |

### ✅ **Progress Check: Data Types**

Before moving on, ensure you can identify:

- [ ] Which type is `25`? (int)
- [ ] Which type is `"25"`? (str - it's in quotes!)
- [ ] Which type is `25.0`? (float)
- [ ] Which type is `True`? (bool)

---

## 🔄 **Part 3: Dynamic Typing**

### **What is Dynamic Typing?**

Python is **dynamically typed** - this means Python figures out the data type automatically based on what you assign.

**You don't need to declare the type!**

```python
# Python automatically knows:
name = "Alice"        # This is a string (str)
age = 25              # This is an integer (int)
height = 5.9          # This is a float
is_student = True     # This is a boolean (bool)
```

**Compare to other languages:**

- Some languages require: `int age = 25;` (you must say it's an int)
- Python just needs: `age = 25` (Python figures it out)

### **Variables Can Change Types**

In Python, a variable can hold different types at different times:

```python
value = 25           # value is an int
value = "Hello"      # Now value is a str
value = 3.14        # Now value is a float
```

**This is allowed in Python!** The variable name stays the same, but what's inside can change type.

### **Why This Matters**

Dynamic typing makes Python:

- ✅ **Easier to learn** - Less to remember
- ✅ **More flexible** - Change types as needed
- ✅ **Faster to write** - No type declarations

**But be careful:** Make sure you know what type your variable is when you use it!

---

## 🔍 **Part 4: Type Checking**

### **Using `type()` Function**

You learned `type()` in Session 1. Now let's use it to check data types:

```python
name = "Alice"
age = 25
height = 5.9
is_student = True

print(type(name))        # <class 'str'>
print(type(age))         # <class 'int'>
print(type(height))      # <class 'float'>
print(type(is_student))  # <class 'bool'>
```

### **Using `isinstance()` Function**

`isinstance()` checks if a value is a specific type (returns `True` or `False`):

```python
age = 25

print(isinstance(age, int))    # True
print(isinstance(age, str))    # False
print(isinstance(age, float))  # False
```

**Why use `isinstance()`?**

- More readable than `type() ==`
- Better for checking types in conditions
- Recommended by Python style guides

**Example:**

```python
value = 25

if isinstance(value, int):
    print("This is an integer!")
else:
    print("This is not an integer!")
```

### ✅ **Progress Check: Type Checking**

Before moving on, ensure you can:

- [ ] Use `type()` to see what type a variable is
- [ ] Use `isinstance()` to check if a variable is a specific type
- [ ] Understand the difference between `type()` and `isinstance()`

---

## 🔄 **Part 5: Type Conversion**

### **What is Type Conversion?**

**Type conversion** (also called **casting**) means changing a value from one data type to another.

### **Converting to Integer (`int()`)**

```python
# Convert string to integer
age_str = "25"
age_int = int(age_str)      # Now it's 25 (int), not "25" (str)

# Convert float to integer (truncates decimal)
price = 19.99
price_int = int(price)      # Becomes 19 (drops .99)
```

**Important:** The string must contain a valid number!

```python
int("25")      # ✅ Works: 25
int("25.5")    # ❌ Error: can't convert "25.5" directly to int
int("hello")   # ❌ Error: "hello" is not a number
```

### **Converting to Float (`float()`)**

```python
# Convert string to float
price_str = "19.99"
price_float = float(price_str)  # Now it's 19.99 (float)

# Convert integer to float
age = 25
age_float = float(age)          # Becomes 25.0 (float)
```

### **Converting to String (`str()`)**

```python
# Convert number to string
age = 25
age_str = str(age)           # Becomes "25" (string)

price = 19.99
price_str = str(price)       # Becomes "19.99" (string)
```

**Why convert to string?**

- To combine with other strings
- To display numbers in messages

```python
age = 25
message = "I am " + str(age) + " years old"
print(message)  # "I am 25 years old"
```

### **Converting to Boolean (`bool()`)**

```python
# Most values convert to True
bool(1)        # True
bool(25)       # True
bool("hello")  # True
bool(3.14)     # True

# Only these convert to False:
bool(0)        # False
bool(0.0)      # False
bool("")       # False (empty string)
bool(False)    # False
bool(None)     # False
```

### **Common Conversion Patterns**

#### **Pattern 1: User Input (Always Strings)**

```python
# input() always returns a string!
age_str = input("Enter your age: ")  # User types: 25
age_int = int(age_str)              # Convert to integer
```

#### **Pattern 2: Combining Numbers and Text**

```python
age = 25
message = "I am " + str(age) + " years old"
# Or use f-strings (easier!):
message = f"I am {age} years old"
```

#### **Pattern 3: Calculations with User Input**

```python
# Get numbers from user
num1_str = input("Enter first number: ")
num2_str = input("Enter second number: ")

# Convert to integers for calculation
num1 = int(num1_str)
num2 = int(num2_str)

# Now we can do math
result = num1 + num2
print(f"The sum is {result}")
```

### ✅ **Progress Check: Type Conversion**

Before moving on, ensure you can:

- [ ] Convert strings to integers with `int()`
- [ ] Convert strings to floats with `float()`
- [ ] Convert numbers to strings with `str()`
- [ ] Understand that `input()` always returns a string
- [ ] Convert user input before doing calculations

---

## 🚀 **Part 6: Hands-on Practice**

### **📁 Complete File Reference:**

**File Location:** `[your-project-folder]\src\L1\S2\` _(replace with your actual path)_  
**Practice Files:** `01_variables.py`, `02_data_types.py`, `03_type_conversion.py`

1. **`01_variables.py`** 🎯 **Learn:** Variable creation, naming, assignment
2. **`02_data_types.py`** 🎯 **Learn:** Working with int, float, str, bool
3. **`03_type_conversion.py`** 🎯 **Learn:** Converting between data types

### **Complete Project Structure**

```text
📁 python-fundamentals/
└── 📁 src/
    └── 📁 L1/
        └── 📁 S2/
            ├── 📄 01_variables.py
            ├── 📄 02_data_types.py
            └── 📄 03_type_conversion.py
```

### **Script 1: 01_variables.py (Variable Basics)**

```python
# Filename: src/L1/S2/01_variables.py

# Session 2: Variables & Data Types
# Practice: Creating and using variables

# Create variables with different data types
name = "Alice"
age = 25
height = 5.9
is_student = True

# Display the variables
print("=== Variable Examples ===")
print(f"Name: {name}")
print(f"Age: {age}")
print(f"Height: {height} feet")
print(f"Is Student: {is_student}")

# Show variable types
print("\n=== Variable Types ===")
print(f"name is {type(name)}")
print(f"age is {type(age)}")
print(f"height is {type(height)}")
print(f"is_student is {type(is_student)}")

# Variables can be reassigned
print("\n=== Reassigning Variables ===")
age = 26  # Changed from 25 to 26
print(f"Updated age: {age}")

# Variables can change types (dynamic typing)
print("\n=== Dynamic Typing Example ===")
value = 25
print(f"value = {value}, type = {type(value)}")

value = "Now I'm a string!"
print(f"value = {value}, type = {type(value)}")
```

### **Running the Script**

#### **Method 1: VS Code (Recommended)**

1. Open `01_variables.py` in VS Code
2. Click ▶️ "Run Python File" button
3. OR press `Ctrl+F5`

#### **Method 2: PowerShell**

```powershell
# Navigate to your project folder
cd [your-project-folder]\src\L1\S2

# Run the script
python 01_variables.py
```

### **Expected Output:**

```text
=== Variable Examples ===
Name: Alice
Age: 25
Height: 5.9 feet
Is Student: True

=== Variable Types ===
name is <class 'str'>
age is <class 'int'>
height is <class 'float'>
is_student is <class 'bool'>

=== Reassigning Variables ===
Updated age: 26

=== Dynamic Typing Example ===
value = 25, type = <class 'int'>
value = Now I'm a string!, type = <class 'str'>
```

### **Script 2: 02_data_types.py (Data Types Deep Dive)**

```python
# Filename: src/L1/S2/02_data_types.py

# Session 2: Variables & Data Types
# Practice: Working with all four data types

print("=== Python's Four Fundamental Data Types ===\n")

# 1. Integers (int)
print("1. INTEGERS (int):")
age = 25
temperature = -10
count = 0
print(f"   age = {age} ({type(age)})")
print(f"   temperature = {temperature} ({type(temperature)})")
print(f"   count = {count} ({type(count)})")

# 2. Floating-Point Numbers (float)
print("\n2. FLOATING-POINT NUMBERS (float):")
height = 5.9
price = 19.99
pi = 3.14159
print(f"   height = {height} ({type(height)})")
print(f"   price = {price} ({type(price)})")
print(f"   pi = {pi} ({type(pi)})")

# 3. Strings (str)
print("\n3. STRINGS (str):")
name = "Alice"
message = 'Hello, World!'
favorite_color = "blue"
print(f"   name = {name} ({type(name)})")
print(f"   message = {message} ({type(message)})")
print(f"   favorite_color = {favorite_color} ({type(favorite_color)})")

# 4. Booleans (bool)
print("\n4. BOOLEANS (bool):")
is_student = True
is_graduated = False
has_license = True
print(f"   is_student = {is_student} ({type(is_student)})")
print(f"   is_graduated = {is_graduated} ({type(is_graduated)})")
print(f"   has_license = {has_license} ({type(has_license)})")

# Using isinstance() to check types
print("\n=== Type Checking with isinstance() ===")
print(f"   isinstance(age, int): {isinstance(age, int)}")
print(f"   isinstance(height, float): {isinstance(height, float)}")
print(f"   isinstance(name, str): {isinstance(name, str)}")
print(f"   isinstance(is_student, bool): {isinstance(is_student, bool)}")
```

### **Script 3: 03_type_conversion.py (Type Conversion Practice)**

```python
# Filename: src/L1/S2/03_type_conversion.py

# Session 2: Variables & Data Types
# Practice: Converting between data types

print("=== Type Conversion Examples ===\n")

# Converting to Integer
print("1. CONVERTING TO INTEGER (int()):")
age_str = "25"
age_int = int(age_str)
print(f"   '{age_str}' (str) → {age_int} (int)")

price_float = 19.99
price_int = int(price_float)
print(f"   {price_float} (float) → {price_int} (int)")

# Converting to Float
print("\n2. CONVERTING TO FLOAT (float()):")
price_str = "19.99"
price_float = float(price_str)
print(f"   '{price_str}' (str) → {price_float} (float)")

age_int = 25
age_float = float(age_int)
print(f"   {age_int} (int) → {age_float} (float)")

# Converting to String
print("\n3. CONVERTING TO STRING (str()):")
age = 25
age_str = str(age)
print(f"   {age} (int) → '{age_str}' (str)")

price = 19.99
price_str = str(price)
print(f"   {price} (float) → '{price_str}' (str)")

# Converting to Boolean
print("\n4. CONVERTING TO BOOLEAN (bool()):")
print(f"   bool(1): {bool(1)}")
print(f"   bool(0): {bool(0)}")
print(f"   bool('hello'): {bool('hello')}")
print(f"   bool(''): {bool('')}")

# Practical Example: User Input
print("\n=== Practical Example: User Input ===")
print("(This would normally come from input(), showing conversion)")
age_input = "25"  # Simulating input("Enter your age: ")
age = int(age_input)
print(f"   User entered: '{age_input}' (string)")
print(f"   Converted to: {age} (integer)")
print(f"   Can now do math: {age} + 5 = {age + 5}")

# Combining strings and numbers
print("\n=== Combining Strings and Numbers ===")
name = "Alice"
age = 25
# Method 1: Using str() conversion
message1 = "I am " + str(age) + " years old"
print(f"   Method 1: {message1}")

# Method 2: Using f-strings (easier!)
message2 = f"I am {age} years old"
print(f"   Method 2: {message2}")
```

### **Mini Practice Challenge**

Create a program that stores and displays your personal details using all four data types:

**Requirements:**

- Use at least one `int` variable
- Use at least one `float` variable
- Use at least one `str` variable
- Use at least one `bool` variable
- Display all information in a formatted way
- Use `type()` to show each variable's type

**Example Structure:**

```python
# Your personal details program
name = "Your Name"           # str
age = 25                     # int
height = 5.9                 # float
is_student = True            # bool

# Display with types
print(f"Name: {name} ({type(name)})")
# ... continue for other variables
```

---

## 📝 **Key Takeaways**

### **What We Learned:**

- ✅ Variables store values with names using the `=` assignment operator
- ✅ Python has four fundamental data types: `int`, `float`, `str`, `bool`
- ✅ Python uses dynamic typing - it figures out types automatically
- ✅ Use `type()` to see what type a variable is
- ✅ Use `isinstance()` to check if a variable is a specific type
- ✅ Convert between types using `int()`, `float()`, `str()`, `bool()`
- ✅ `input()` always returns a string - convert before doing math!

### **Python Concepts Introduced:**

- **Variables:** `name = "Alice"`
- **Data Types:**
  - **`int`:** `25`, `-10`, `0`
  - **`float`:** `3.14`, `-5.5`, `0.0`
  - **`str`:** `"Hello"`, `'World'`
  - **`bool`:** `True`, `False`
- **Type Checking:**
  - **`type()`:** Returns the type of a value
  - **`isinstance()`:** Checks if value is a specific type
- **Type Conversion:**
  - **`int()`:** Convert to integer
  - **`float()`:** Convert to float
  - **`str()`:** Convert to string
  - **`bool()`:** Convert to boolean

---

## 🔧 **Troubleshooting**

### **Common Issues:**

#### **"NameError: name 'variable' is not defined"**

**Problem:** You're trying to use a variable before creating it.

```python
# ❌ Wrong - using variable before creating it
print(name)
name = "Alice"

# ✅ Correct - create variable first
name = "Alice"
print(name)
```

#### **"ValueError: invalid literal for int()"**

**Problem:** Trying to convert a string that isn't a valid number.

```python
# ❌ Wrong - "hello" can't be converted to int
age = int("hello")

# ✅ Correct - use a valid number string
age = int("25")
```

#### **"TypeError: can only concatenate str (not 'int') to str"**

**Problem:** Trying to combine string and number without conversion.

```python
# ❌ Wrong - can't add string and int directly
age = 25
message = "I am " + age + " years old"

# ✅ Correct - convert number to string
message = "I am " + str(age) + " years old"

# ✅ Or use f-strings (easier!)
message = f"I am {age} years old"
```

#### **Variable name errors**

**Problem:** Using invalid characters or Python keywords.

```python
# ❌ Wrong - space in variable name
first name = "Alice"

# ❌ Wrong - starting with number
2nd_place = "Bob"

# ❌ Wrong - using Python keyword
print = "Hello"

# ✅ Correct - use valid names
first_name = "Alice"
second_place = "Bob"
message = "Hello"
```

---

**🎉 Congratulations! You've completed Session 2!**  
_You now understand variables and data types - the building blocks of Python programming!_

**Next Up:** Session 3 will teach you how to work with these data types using operators and expressions.
