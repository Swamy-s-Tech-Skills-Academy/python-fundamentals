---
learning_level: "Noob → Nerd"
prerequisites: 
  - "docs/sessions/L1/01_S1.md"
  - "docs/sessions/L1/02_S2.md"
  - "docs/sessions/L1/03_S3.md"
  - "docs/sessions/L1/04_S4.md"
estimated_time: "30-45 minutes"
session_type: "Project"
learning_objectives:
  - "Apply all Phase A concepts in a real project"
  - "Build a functional command-line calculator"
  - "Implement user input handling and validation"
  - "Use conditionals for operation selection"
  - "Create a program loop that runs until user quits"
related_topics:
  prerequisites:
    - "docs/sessions/L1/04_S4.md"
  builds_upon:
    - "docs/sessions/L1/01_S1.md"
    - "docs/sessions/L1/02_S2.md"
    - "docs/sessions/L1/03_S3.md"
    - "docs/sessions/L1/04_S4.md"
  enables:
    - "docs/sessions/L1/06_S5.md"
  cross_refs: []
---

# 🚀 Mini Project 1: Simple Calculator

**Duration:** 30-45 minutes  
**Type:** 🛠️ Project  
**Level:** Noob → Nerd

---

## 🎯 **Project Overview**

### **What You'll Build:**

A command-line calculator that performs basic arithmetic operations. This project combines everything you've learned in Phase A:

- ✅ User input and output (Session 1)
- ✅ Variables and data types (Session 2)
- ✅ Arithmetic and comparison operators (Session 3)
- ✅ Conditionals for operation selection (Session 4)

### **Calculator Features:**

- ➕ Addition
- ➖ Subtraction
- ✖️ Multiplication
- ➗ Division (with zero-check)
- 🔄 Runs continuously until user quits
- ✅ Input validation
- 🎨 Clean, formatted output

---

## 📋 **Before You Begin**

### **Prerequisites:**

✅ Completed Sessions 1-4  
✅ Understand `input()`, `print()`, and f-strings  
✅ Know how to use arithmetic operators  
✅ Can write `if-elif-else` statements  
✅ Understand type conversion (`int()`, `float()`)

---

## 🗺️ **Project Roadmap**

| Step | Task                         | Concepts Used                    |
| ---- | ---------------------------- | -------------------------------- |
| 1    | Display welcome & menu       | `print()`, string formatting     |
| 2    | Get user's operation choice  | `input()`, conditionals          |
| 3    | Get two numbers              | `input()`, `float()` conversion  |
| 4    | Perform calculation          | Arithmetic operators             |
| 5    | Display result               | f-strings, formatting            |
| 6    | Loop until quit              | `while` loop (preview!)          |
| 7    | Add input validation         | `if` statements, error handling  |

---

## 💻 **Building the Calculator**

### **Step 1: Project Setup**

Create a new file: `src/L1/MP1/simple_calculator.py`

```text
📁 src/
└── 📁 L1/
    └── 📁 MP1/
        └── 📄 simple_calculator.py
```

---

### **Step 2: Basic Structure**

Start with the welcome message and menu:

```python
# Filename: src/L1/MP1/simple_calculator.py

print("=" * 40)
print("🧮 SIMPLE CALCULATOR 🧮")
print("=" * 40)
print("\nOperations:")
print("  1. Addition (+)")
print("  2. Subtraction (-)")
print("  3. Multiplication (*)")
print("  4. Division (/)")
print("  5. Quit")
print()
```

---

### **Step 3: Get User Input**

Get the operation choice and numbers:

```python
# Get operation choice
choice = input("Enter operation (1-5): ")

# Get numbers
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))
```

---

### **Step 4: Perform Calculations**

Use conditionals to perform the right operation:

```python
if choice == "1":
    result = num1 + num2
    operation = "+"
elif choice == "2":
    result = num1 - num2
    operation = "-"
elif choice == "3":
    result = num1 * num2
    operation = "*"
elif choice == "4":
    if num2 != 0:
        result = num1 / num2
        operation = "/"
    else:
        print("❌ Error: Cannot divide by zero!")
        result = None
        operation = None
else:
    print("❌ Invalid choice!")
    result = None
    operation = None

# Display result
if result is not None:
    print(f"\n✨ Result: {num1} {operation} {num2} = {result}")
```

---

### **Step 5: Add a Loop (Preview of Session 5!)**

Make the calculator run continuously:

```python
while True:
    # Show menu
    print("\n" + "=" * 40)
    print("🧮 SIMPLE CALCULATOR 🧮")
    print("=" * 40)
    print("\nOperations:")
    print("  1. Addition (+)")
    print("  2. Subtraction (-)")
    print("  3. Multiplication (*)")
    print("  4. Division (/)")
    print("  5. Quit")
    
    choice = input("\nEnter operation (1-5): ")
    
    # Check for quit
    if choice == "5":
        print("\n👋 Thanks for using the calculator! Goodbye!")
        break  # Exit the loop
    
    # ... rest of the calculation code
```

---

## 📄 **Complete Solution**

Here's the full calculator with all features:

```python
# Filename: src/L1/MP1/simple_calculator.py

"""
🧮 Simple Calculator - Mini Project 1
Level 1: Noob → Nerd

A command-line calculator demonstrating:
- User input and output
- Variables and data types
- Arithmetic operators
- Conditional statements
- Basic loop structure
"""

def show_menu():
    """Display the calculator menu."""
    print("\n" + "=" * 40)
    print("🧮 SIMPLE CALCULATOR 🧮")
    print("=" * 40)
    print("\nOperations:")
    print("  1. Addition (+)")
    print("  2. Subtraction (-)")
    print("  3. Multiplication (*)")
    print("  4. Division (/)")
    print("  5. Power (**)")
    print("  6. Modulo (%)")
    print("  7. Quit")


def get_numbers():
    """Get two numbers from user with validation."""
    while True:
        try:
            num1 = float(input("\nEnter first number: "))
            num2 = float(input("Enter second number: "))
            return num1, num2
        except ValueError:
            print("❌ Invalid input! Please enter numbers only.")


def calculate(choice, num1, num2):
    """Perform calculation based on user choice."""
    if choice == "1":
        return num1 + num2, "+"
    elif choice == "2":
        return num1 - num2, "-"
    elif choice == "3":
        return num1 * num2, "*"
    elif choice == "4":
        if num2 != 0:
            return num1 / num2, "/"
        else:
            print("❌ Error: Cannot divide by zero!")
            return None, None
    elif choice == "5":
        return num1 ** num2, "**"
    elif choice == "6":
        if num2 != 0:
            return num1 % num2, "%"
        else:
            print("❌ Error: Cannot calculate modulo with zero!")
            return None, None
    else:
        return None, None


def main():
    """Main calculator loop."""
    print("\n🎉 Welcome to Simple Calculator!")
    print("This calculator was built in Level 1, Phase A.")
    
    while True:
        show_menu()
        choice = input("\nEnter operation (1-7): ")
        
        # Check for quit
        if choice == "7":
            print("\n" + "=" * 40)
            print("👋 Thanks for using the calculator!")
            print("Happy coding! 🐍✨")
            print("=" * 40)
            break
        
        # Validate choice
        if choice not in ["1", "2", "3", "4", "5", "6"]:
            print("❌ Invalid choice! Please enter 1-7.")
            continue
        
        # Get numbers
        num1, num2 = get_numbers()
        
        # Calculate
        result, operation = calculate(choice, num1, num2)
        
        # Display result
        if result is not None:
            print("\n" + "-" * 40)
            print(f"✨ {num1} {operation} {num2} = {result}")
            print("-" * 40)


# Run the calculator
if __name__ == "__main__":
    main()
```

---

## 🎯 **Running Your Calculator**

### **Method 1: VS Code**

1. Open `simple_calculator.py`
2. Click ▶️ "Run Python File" or press `Ctrl+F5`

### **Method 2: Terminal**

```powershell
cd src/L1/MP1
python simple_calculator.py
```

### **Expected Output:**

```text
🎉 Welcome to Simple Calculator!
This calculator was built in Level 1, Phase A.

========================================
🧮 SIMPLE CALCULATOR 🧮
========================================

Operations:
  1. Addition (+)
  2. Subtraction (-)
  3. Multiplication (*)
  4. Division (/)
  5. Power (**)
  6. Modulo (%)
  7. Quit

Enter operation (1-7): 1

Enter first number: 10
Enter second number: 5

----------------------------------------
✨ 10.0 + 5.0 = 15.0
----------------------------------------
```

---

## ✅ **Project Success Checklist**

Your calculator should:

- [ ] Display a clear menu of operations
- [ ] Accept user input for operation choice
- [ ] Accept two numbers from the user
- [ ] Perform the correct calculation
- [ ] Handle division by zero gracefully
- [ ] Display formatted results
- [ ] Run continuously until user quits
- [ ] Handle invalid input without crashing

---

## 🌟 **Stretch Goals (Optional)**

If you finish early, try adding these features:

### **1. Memory Feature**

```python
# Store last result
memory = 0

# Add menu option: "8. Use last result"
if choice == "8":
    num1 = memory
    print(f"Using memory value: {memory}")
```

### **2. Square Root**

```python
# Add menu option for square root (single number)
elif choice == "9":
    num = float(input("Enter number: "))
    if num >= 0:
        result = num ** 0.5
        print(f"√{num} = {result}")
    else:
        print("Cannot calculate square root of negative number!")
```

### **3. Calculation History**

```python
# Keep track of calculations
history = []

# After each calculation:
history.append(f"{num1} {operation} {num2} = {result}")

# Add menu option to show history
if choice == "H":
    print("\n📜 Calculation History:")
    for calc in history:
        print(f"  {calc}")
```

---

## 📝 **What You've Accomplished**

By completing this project, you've demonstrated:

- ✅ **Session 1 Skills:** Input/output, print formatting
- ✅ **Session 2 Skills:** Variables, data types, type conversion
- ✅ **Session 3 Skills:** All arithmetic operators
- ✅ **Session 4 Skills:** Conditionals, decision making

### **Key Patterns Learned:**

1. **Menu-driven programs** - Display options, get choice, execute
2. **Input validation** - Check for valid input before processing
3. **Error handling** - Handle edge cases like division by zero
4. **Program loops** - Keep running until user decides to quit

---

## 🎓 **Next Steps**

You've completed Phase A! 🎉

**Coming up in Phase B:**

- Session 5: Loops & Iteration (master `for` and `while`)
- Session 6: Debugging & Built-in Functions
- Session 7: Lists & Loops
- Session 8: Dictionaries & Basic Testing
- Mini Project 2: Personal Profile Generator

---

**🎉 Congratulations on completing Mini Project 1!**  
_You've built your first real Python application!_ 🐍✨
