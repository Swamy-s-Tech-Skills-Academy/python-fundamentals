# Primary Directives

**Version**: 1.0  
**Last Updated**: December 2025

---

## 🎯 Primary Directives

### 1. Python Fundamentals Educational Content Rules (MANDATORY)

**All content creation must follow these rules without exception.**

See: `01_educational-content-rules.mdc` for complete details.

### 2. Automation-First Approach

✅ **ALWAYS**: Create reusable PowerShell scripts for repetitive tasks  
✅ **ALWAYS**: Use existing scripts in `scripts/` first  
✅ **ALWAYS**: Enhance existing scripts rather than duplicating  
❌ **NEVER**: Execute individual commands for tasks repeated 2+ times

**Decision Tree**:

1. Check `scripts/` for existing script
2. Enhance existing script if close match
3. Create new `.ps1` for any repeated task
4. Document with examples and parameters

**Available Automation** (scripts/):

- `docs-lint.ps1` - Markdown linting script
- `docs-links.ps1` - Link validation script
- `show-tree.ps1` - Repository structure generator

### 3. Update Verification Protocol (CRITICAL)

**After ANY structural change, IMMEDIATELY update**:

1. ✅ `.github/copilot-instructions.md` - Repository Structure section
2. ✅ `README.md` - Repository Structure section
3. ✅ Relevant documentation files
4. ✅ Test all Python code files still work
5. ✅ Run markdownlint: `npx --yes markdownlint-cli2 "docs/**/*.md"`

**Self-Check Question**: "Did I update the instruction file?" - If no, STOP and do it NOW.

### 4. File Naming Validation (CRITICAL)

**Before creating or committing ANY numbered file, validate naming**:

1. ✅ **Python files**: Use `01_`, `02_`, etc. prefixes (e.g., `01_hello.py`)
2. ✅ **Session docs**: Use existing `S1.md`, `S2.md`, `S5.md`, `S10.md`, and `_Plan.md` naming
3. ✅ **Directory structure**: Use `L{level}/S{session}/` format
4. ✅ **Verify paths**: All references use correct `L{level}/S{session}/` structure

**Self-Check Question**: "Do all file references use the correct L{level}/S{session}/ format?" - If no, FIX immediately.

### 4a. Python Template Validation (CRITICAL)

**Before creating or refactoring ANY Python file, validate the template choice**:

1. ✅ Check `docs/PythonFileTemplates.md` first
2. ✅ Use the CLI Lesson Script template for runnable Working samples and later-session lesson scripts unless the file is intentionally simpler or import-oriented
3. ✅ Use the Teaching Script template only when top-level linear execution is part of the pedagogy
4. ✅ Use the Helper/App template for importable helpers and application-style entry points

**Self-Check Question**: "Am I using the correct template from docs/PythonFileTemplates.md for this file's purpose?" - If no, FIX immediately.

### 5. Internal Intake Policy Placement (CRITICAL)

✅ Maintain source-intake and sandbox-promotion policy in internal instruction surfaces only (`.github/`, `.cursor/rules/`, `CLAUDE.md`)  
✅ Keep publish-facing curriculum docs focused on learner content, outcomes, and exercises  
❌ Do not publish internal process-policy blocks in `docs/` content

### 6. Completed Session Protection (CRITICAL)

✅ Treat `docs/meetup/L1/sessions.md` as the session-status guard for meetup-facing placement decisions  
✅ Bucket new content into planned/new sessions by default  
❌ Do not add newly authored content into completed sessions unless explicit user permission is received in the current request  
✅ If status or destination is uncertain, ask before editing completed-session content

### 7. Hands-off: `src/Working/` (CRITICAL)

❌ Do **not** create, edit, move, rename, or delete files under `src/Working/` **unless Swamy explicitly requests** that Working path or folder in the current task  
✅ Use formal `src/L{level}/S{session}/` and `docs/sessions/` for routine curriculum work  
✅ You may **read** `docs/RepositoryStructure.md` (section **src/Working/**) or other **non-modifying** views of Working files for routing context when explaining promotion — without modifying Working unless asked  
❌ Do not include `src/Working/` in mass refactors or repo-wide formatting unless the task scope names it
