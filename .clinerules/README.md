# Cline rules — pointer

**Canonical policy:** root [`AGENTS.md`](../AGENTS.md) and [`.cursor/rules/`](../.cursor/rules/).

This folder provides a **Cline-facing entry** for the Python Fundamentals curriculum repository. Do not treat mirrors here as overriding root `.cursor/` files.

| Path | Purpose |
| --- | --- |
| [`AGENTS.md`](AGENTS.md) | Cline entry index |
| [`agents/`](agents/) | Curriculum-focused subagent mirrors |
| Root `.cursor/rules/` | Authoritative `.mdc` rules |
| Root `.cursor/skills/` | Project skill pack |
| Root `.cursor/agents/` | Cursor subagents |

**Repository:** `python-fundamentals` — L1 and L2 formal curriculum, meetup docs, pytest suite.
