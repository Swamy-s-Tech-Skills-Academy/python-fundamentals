# Cline rules — pointer

**Canonical policy:** root [`AGENTS.md`](../AGENTS.md) and [`.cursor/rules/`](../.cursor/rules/).

Cline-facing mirrors for the **python-fundamentals** curriculum repository.

| Path | Purpose |
| --- | --- |
| [`AGENTS.md`](AGENTS.md) | Cline entry index |
| [`agents/`](agents/) | Subagents: CI verify, session roadmap, practice code, docs originality |
| [`skills/`](skills/) | CI, docs verification, workspace review mirrors |
| [`workflows/`](workflows/) | Repeatable audit workflows (`session-roadmap-review`, `workspace-review`, `run-ci-checks`) |
| Root `.cursor/rules/` | Authoritative `.mdc` rules |
| Root `.opencode/` | OpenCode plugin mirrors (same policies) |

**Repository:** formal L1–L2 curriculum, L3–L18 level plans, pytest suite, `scripts/` doc helpers.
